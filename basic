关于时间：
a= datetime.datetime()
a.weekday()

--------------------------------
c=dict(zip(a,b))

.sort(reverse=True)

‘ ’。join() 将序列中的元素以指定的字符拼接接生成一个新的字符串。
语法： ‘sep’.join(seq)
参数说明：
sep：分隔符。可以为空
seq：要连接的元素序列、字符串、元组、字典

返回值
返回通过指定字符连接seq中的元素后生成的新字符串。
